async function loadCharacters() {
  const index = await fetch("data/characters/index.json").then(r => r.json());
  const characters = [];

  for (const path of index) {
    const json = await fetch(`data/characters/${path}`).then(r => r.json());
    characters.push(json);
  }

  return characters;
}
